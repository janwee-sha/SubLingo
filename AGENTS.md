# Agent 工作协议

## 项目宪法

[`docs/engineering/constitution.md`](docs/engineering/constitution.md) 文件描述了项目宪法。

## 工作原则

### I. 轻量双轨交付

每项变更 MUST 使用足以安全维护产品意图的最轻流程。

- 新增面向用户的能力、跨运行时变更、安全或隐私边界变更、持久化数据变更或大范围架构变更 MUST 使用完整 SDD 轨道：
  规格、计划、可执行任务、一致性审查、实现和验证。
- 局部缺陷修复、文档纠正、依赖维护、仅测试变更或行为不变的重构 SHOULD 使用轻量轨道：简短的问题说明、直接实现和与风险相称的验证。
- 轻量变更一旦超出原定边界，MUST 在继续扩大实现范围前升级为完整 SDD 轨道。

流程的目的在于降低不确定性和风险，而不是强迫每次编辑生成同一套文档。

### II. SDD 产物描述当前意图

SDD 产物 MUST 只描述功能当前的预期、设计和仍待完成的工作，MUST NOT 充当开发日记、变更日志或只增不减的补救记录。

- 被替代的需求、放弃的设计、过时任务和历史实现路径 MUST 直接移除，MUST NOT 通过“旧版”“最初”或“已替代”等注释继续保留。
- `tasks.md` MUST 保留全部任务；已验收任务 MUST 标记为 `[X]`，未验收任务 MUST 保持 `[ ]`。
  已验收任务 MUST NOT 因功能收敛而被删除、压缩或重新编号。
- 功能收敛后发现的大型补救工作 MUST 成为新的有界功能或轻量变更，MUST NOT 作为永久
  阶段继续追加到旧任务列表。
- Git 历史、议题、拉取请求、发布说明和验证证据是权威历史记录；SDD 文档不是。

### III. 简明扼要、职责单一且中文优先的上下文

治理项目的 SDD 约束、项目文档时 MUST NOT 长篇大论，每份产物 MUST 只包含履行自身职责所需的信息；
由其他产物负责的细节 MUST 通过引用关联，MUST NOT 重复抄写。

项目宪法、SDD 产物和 `docs/` 下的项目文档 MUST 默认使用中文。仅当翻译会降低准确性、 可执行性或可追溯性时，
引用原文、ID、MUST/NOT 等约束谓词、代码、命令、文件路径、 协议字段、产品名和其他必要的专有名称 MAY 保留英文。
普通标题、说明、状态、步骤和表格列名 MUST 使用中文，MUST NOT 因模板原文为英文而保留英文套话。

文档 MUST 足够简洁，使 Agent 能载入相关功能上下文而不挤占源码、测试或诊断输出的空间。无法保持简洁的功能 MUST 拆分为可独立测试的切片。
分配给 Agent 的上下文 SHOULD 只包含其负责切片所需的产物章节和代码。

### IV. 明确并行工作边界

并行执行 MUST 依据真实依赖和文件所有权，MUST NOT 只依据 `[P]` 标记。

- 任务仅在不依赖未完成的同级工作，且不会与其他并发任务修改相同文件时 MAY 标记为 `[P]`。
- 并行处理独立功能或切片的 Agent MUST 使用隔离的 worktree 或等效隔离工作区。
- 共享集成文件和其他热点文件 MUST 同一时间只有一个负责人，或事先规定明确的合并顺序。
- 每项委派任务 MUST 说明范围、相关契约、允许修改的文件、验证命令和完成条件。

## 交付轨道选择

出现以下任一情况时使用完整 SDD 轨道：

- 变更新增或实质改变用户故事；
- 变更公共消息、服务提供方、存储、传输、打包、隐私或安全契约；
- 变更跨越多个运行时负责人，或需要多个子系统协同；
- 重要范围或验收行为仍有歧义；
- 独立 Agent 在实现前需要持久、共享的契约。

当变更有界、可逆，且简短问题说明和测试足以完整描述时使用轻量轨道。变更负责人 MUST
在任务、议题或工作说明中记录所选轨道，并在范围扩大时 MUST 重新评估。

## 开发工作流

完整 SDD 工作：

`constitution`（建项或治理变更时） -> `specify` -> `clarify`（按需） -> `plan` -> `checklist`（按需） ->
`tasks` -> `analyze`（按需） -> `taskstoissues`（按需） -> `implement` -> 验证。

- `constitution`：仅在创建或修订项目治理原则时执行；普通功能沿用现有宪法。
- `specify`：定义当前功能范围、用户故事、需求和验收标准。
- `clarify`：在规划前消除会影响范围、验收或设计的重要歧义；规格已无实质歧义时 MAY 省略。
- `plan`：形成符合宪法约束的技术方案、契约和验证方法。
- `checklist`：按需生成需求质量检查清单；不需要额外领域质量门时 MAY 省略，但 MUST 在实施前完成所有已采用清单的检查。
- `tasks`：把规格和计划拆分为有依赖顺序、可独立验收的可执行任务。
- `analyze`：在 `tasks` 完成后只读检查 `spec.md`、`plan.md` 与 `tasks.md` 的一致性和覆盖；产物简单且已确认一致时 MAY 省略。
- `taskstoissues`：仅在决定用 GitHub Issues 承载任务时执行；否则 MAY 省略。
- `implement`：按 `tasks.md` 实施并标记已验收任务，随后执行与风险相称的自动化和人工验证。

完整 SDD 工作 MUST 按以下阶段顺序执行。每个计划性阶段完成且不存在待澄清项时，AI Agent MAY 自动进入下一个计划性阶段；
存在待澄清项时，MUST 停止推进并取得用户的批准或调整意见，再根据反馈继续工作流或返工调整。即使全部计划性阶段和一致性审查均已完成，
进入规格实施前仍 MUST 获得用户明确指示；用户授权规划工作或允许自动推进计划性阶段，MUST NOT 视为实施授权。

轻量工作：

1. 简明说明问题、预期行为和受影响边界。
2. 实现最小且完整的变更。
3. 运行聚焦的回归测试，以及风险所要求的更广泛检查。
4. 出现新的产品范围或架构不确定性时，升级为完整 SDD。

开发验收阶段需要实机验证时，AI Agent MUST 优先提供明确的手动测试步骤并指示用户完成测试。
只有手动测试遇到不可克服的阻力而无法完成时，AI Agent MAY 调用 Computer Use 等工具自动化执行测试。

审查 MUST 拒绝只为流程而增长的文档、重复上下文、过时需求，以及混合当前工作与历史叙事的任务列表。

## 已发现 Bug 清单

[`docs/open_issues`](docs/open_issues) 目录记录了已发现的 Bug，只在用户指示修复时再读取对应文档。

## 翻译服务 Provider

[`docs/providers`](docs/providers) 目录提供了用于测试验证的翻译服务的 API 信息，不应将它们写入项目任何其他位置且不要版本化这些信息。

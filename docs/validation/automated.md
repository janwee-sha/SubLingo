# Automated validation — 2026-08-11

All commands were run from the repository root on macOS 26.5.2 (arm64) with Node.js 24.18.0, npm 11.16.0, and Apple Swift 6.3.3.

| Check                                               | Result           | Evidence                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dependency lock                                     | PASS             | `package.json` and `package-lock.json` are synchronized at version `0.1.2`; `@noble/ciphers` is absent.                                                                                                                                                                                                                                                        |
| TypeScript                                          | PASS             | `npm run typecheck` completed both plugin and WebView projects with no diagnostics.                                                                                                                                                                                                                                                                            |
| ESLint                                              | PASS             | `npm run lint` completed with no diagnostics.                                                                                                                                                                                                                                                                                                                  |
| Formatting                                          | PASS             | `npm run format:check` reported that all matched files use Prettier formatting.                                                                                                                                                                                                                                                                                |
| JavaScript/unit/integration/security/contract tests | PASS             | `npm test`: 31 files and 129 tests passed; the two opt-in live-provider tests were skipped by the default run. Coverage includes fixed-path local credentials, helper health/restart, no provider POST replay, system/direct route identity, two-item OpenAI/Ollama wire splitting, profile update/delete, UI operation state, and playback/session isolation. |
| Native transport contracts                          | PASS             | `npm run test:native`: `SubLingo transport contract tests passed`. The real native store returned JSON `null` for a missing key, round-tripped and deleted one key, and created `credentials.json` with POSIX mode `0600`. Health accepts literal `{}` plus IINA 1.4.4's zero-byte encoding, while rejecting bodies containing fields.                         |
| Final helper smoke                                  | PASS             | The final universal helper returned health `ok`, wrote/read/deleted a dummy credential in a temporary `0600` file, then used `proxyMode: direct` to reach `https://omni.janwee.blog/v1/chat/completions` without a key and received the expected upstream HTTP 401. The temporary directory was removed.                                                       |
| Live Ollama six-cue adapter                         | PASS             | The production Ollama adapter probed `translategemma:12b` and returned all six original cue IDs. This exercised three ordered two-item `/api/chat` requests.                                                                                                                                                                                                   |
| Live OpenAI-compatible six-cue adapter              | UPSTREAM BLOCKED | The authorized probe reached the configured service, which returned HTTP 503 with safe provider code `ALL_ACCOUNTS_INACTIVE`. Endpoint parsing and transport succeeded, but a successful translation cannot be claimed until the service has an active upstream account.                                                                                       |
| Universal helper build                              | PASS             | `npm run build:native` produced a 775,184-byte universal Mach-O with arm64 and x86_64 slices, executable mode and a valid ad-hoc signature. It links system libcurl for explicit no-proxy direct requests. `nm` finds no `SecItem`, `SecAccess`, `SecKeychain` or `SecTrustedApplication` symbol; Security.framework is linked only for system CSPRNG use.     |
| Plugin bundle                                       | PASS             | `npm run build` produced `dist/main.js` 113,581 bytes, `dist/global.js` 134,840 bytes, Sidebar HTML 5,233 bytes, CSS 4,804 bytes and classic-script JS bundles 18,948/2,500 bytes.                                                                                                                                                                             |
| Package verification                                | PASS             | `npm run verify:package` verified required files, executable mode, arm64+x86_64 slices, strict code-signature validity, forbidden runtime/credential files, secret patterns and unsupported CommonJS references.                                                                                                                                               |
| IINA 1.4.4 installed 0.1.1 diagnosis                | FAIL / FIXED     | The formal 0.1.1 package installed with an enabled Uninstall button and exact packaged hashes. Ollama Test launched the helper and connected over loopback, but IINA omitted the serialized `{}` health body; the strict 0.1.1 handler returned HTTP 400. The new native regression and 0.1.2 handler cover the observed zero-byte request.                    |
| Release archive                                     | PASS             | `npm run pack` produced and inspected the minimal 12-entry `SubLingo-0.1.2.iinaplgz` archive documented in `package.md`.                                                                                                                                                                                                                                       |

The complete default Vitest result was `Test Files 31 passed | 1 skipped (32)` and `Tests 129 passed | 2 skipped (131)`.

# Automated validation — 2026-08-10

All commands were run from the repository root on macOS 26.5.2 (arm64) with Node.js 24.18.0, npm 11.16.0, and Apple Swift 6.3.3.

| Check                                               | Result | Evidence                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dependency lock refresh                             | PASS   | `npm install --package-lock-only --ignore-scripts` completed successfully.                                                                                                                                                                                                                                  |
| TypeScript                                          | PASS   | `npm run typecheck` completed both plugin and webview projects with no diagnostics.                                                                                                                                                                                                                         |
| ESLint                                              | PASS   | `npm run lint` completed with no diagnostics.                                                                                                                                                                                                                                                               |
| Formatting                                          | PASS   | `npm run format:check` reported that all matched files use Prettier formatting.                                                                                                                                                                                                                             |
| JavaScript/unit/integration/security/contract tests | PASS   | `npm test`: 29 files passed, 78 tests passed; the opt-in two-test live-provider suite was skipped by the normal offline run.                                                                                                                                                                                |
| Live OpenAI-compatible and Ollama tests             | PASS   | With `SUBLINGO_LIVE_PROVIDER_TEST=1`, the production provider adapters completed a real probe and translation against both user-supplied test services. OpenAI-compatible completed in 5,246 ms; Ollama completed in 13,224 ms after loopback endpoint shorthand normalization. No secret value was logged. |
| Acceptance metrics                                  | PASS   | The suite includes the controlled provider server and asserts first-batch latency, 95%-before-display readiness, zero pause, bounded calls, Retry-After handling, cache reuse, and two-window isolation.                                                                                                    |
| Native transport contracts                          | PASS   | `npm run test:native`: `SubLingo transport contract tests passed`. The local Command Line Tools installation exposes neither XCTest nor Swift Testing, so the script compiles and runs the same production transport sources with a standalone assertion runner.                                            |
| Universal helper build                              | PASS   | `npm run build:native` built arm64 and x86_64 release binaries, combined them, preserved mode `-rwxr-xr-x`, and applied an ad-hoc signature. Final helper size: 547,392 bytes.                                                                                                                              |
| Plugin bundle                                       | PASS   | `npm run build`: `dist/main.js` 102.08 kB, `dist/global.js` 230.04 kB, sidebar HTML 4.57 kB, CSS 4.25 kB, JS 8.56 kB. Runtime dependencies are embedded; no `require(` or `module.exports` remains.                                                                                                         |
| Package verification                                | PASS   | `npm run verify:package` verified required files, executable mode, arm64+x86_64 slices, strict code-signature validity, forbidden runtime files, secret patterns, and CommonJS runtime references.                                                                                                          |
| Release archive                                     | PASS   | `npm run pack` produced and inspected the minimal 11-entry `.iinaplgz` archive documented in `package.md`.                                                                                                                                                                                                  |

The complete default Vitest result was: `Test Files 29 passed | 1 skipped (30)` and `Tests 78 passed | 2 skipped (80)`. The two skipped tests are the intentionally opt-in live-provider probes recorded separately above.
